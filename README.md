# VR API Test


### Setup:

1. Instalar o bundler pelo terminal
  ```
  gem install bundler
  ```
2. Instalar as dependencias do projeto
  ```
  bundle install
  ```

### Rodar feature:

  ```
  cucumber -t@vrpat  
  ```